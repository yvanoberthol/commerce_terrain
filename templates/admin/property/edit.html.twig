{% extends 'base.html.twig' %}
{% block body %}
    <div class="container mt-2 w-75 bg-body">
        <h2 class="text-center w-100" style="text-decoration: underline;">
            Editer le bien <span class="text-info text-capitalize">{{ property.title }}</span>
        </h2>
        {{ include ('admin/property/_form.html.twig',{form: form, button:'Editer',icon:'edit'}) }}
    </div>
{% endblock %}